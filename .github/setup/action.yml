name: Setup
description: "Setup repository"
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: 22
    - uses: pnpm/action-setup@master
      with:
        version: 10
        run_install: true
        cache: true
