name: Setup
description: "Setup repository"
runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@master
      with:
        version: 10
        cache: true
    - uses: actions/setup-node@v6
      with:
        node-version: 22
    - run: pnpm ci
      shell: bash
